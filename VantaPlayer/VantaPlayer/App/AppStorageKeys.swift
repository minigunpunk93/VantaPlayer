import Foundation

enum AppStorageKeys {
    static let isCompactMode = "ui.compact.mode"
    static let isPlaylistVisible = "ui.playlist.visible"
    static let isInspectorVisible = "ui.inspector.visible"
    static let densityMode = "ui.density.mode"
}
